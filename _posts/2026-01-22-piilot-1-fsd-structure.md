---
# ====== 기본 Front Matter ======
title: "[PIILOT] FSD 폴더구조 이해하기"
description: "Feature-Sliced Design(FSD) 아키텍처의 기본 개념과 계층 구조 이해하기"
date: 2026-01-22 09:00:00 +0900
tags: [fsd, 폴더구조, 아키텍처]   # 소문자 권장
categories: [piilot, project]   # 최대 2개 권장
pin: false                 # 홈 상단 고정
toc: true                  # 우측 TOC 표시
comments: false             # 댓글
render_with_liquid: true  # 코드블록 안의 Liquid 무력화(필요 시 true로)
math: false                 # 수식
mermaid: true              #  다이어그램
lang: "ko"
---
FSD 공식 문서: <https://feature-sliced.design/kr/>

## FSD란 무엇인가?

FSD(Feature-Sliced Design)는 **기능 분할 설계**의 약자로, 프론트엔드 애플리케이션의 코드를 **사용자 기능 중심**으로 체계적으로 구성하기 위한 아키텍처 방법론이다.

> **아키텍처 방법론이란?**  
> 소프트웨어 시스템의 전체 구조(구성 요소, 관계, 상호작용)를 정의하고 설계하는 체계적인 접근 방식이다.
{: .prompt-tip }

### 왜 FSD를 사용할까?

프로젝트가 커질수록 "이 코드를 어디에 넣어야 할까?"라는 고민이 늘어난다. FSD는 이런 고민을 줄여주는 **표준화된 폴더 구조**를 제공한다.

#### FSD의 주요 장점

1. **명확한 구조**
   - '레이어-슬라이스-세그먼트'의 표준화된 구조로 코드 위치를 쉽게 파악할 수 있다.
   - "이 코드를 어디에 넣어야 할까?" 고민 시간을 줄여준다.

2. **개발 효율성 향상**
   - 기능별로 독립적인 모듈을 구성하여 개발 속도를 높인다.
   - 팀원 간 동시 작업 시 충돌을 줄여준다.

3. **유지보수 용이**
   - 특정 기능에 대한 변경이 다른 기능에 미치는 영향을 최소화한다.
   - 버그 발생률을 낮추고 유지보수를 쉽게 만든다.

4. **코드 재사용성 증가**
   - 기능별로 분리된 코드가 다른 곳에서 재사용될 가능성이 높아진다.
   - 관심사 분리(Separation of Concerns) 원칙을 적용해 코드 품질을 향상시킨다.

5. **일관성**
   - 구조가 표준화되어 팀 간 협업과 신규 멤버 온보딩이 쉬워진다.

6. **격리성**
   - Layer와 Slice 간 의존성을 제한하여, 특정 모듈만 안전하게 수정할 수 있다.

7. **도메인 중심 구조**
   - 비즈니스 용어 기반의 구조로 되어 있어, 전체 코드를 몰라도 특정 기능을 독립적으로 구현할 수 있다.

## FSD의 3단계 구조

FSD는 **3단계 구조**로 코드를 체계적으로 정리한다:

```
Layer(레이어) → Slice(슬라이스) → Segment(세그먼트)
```

이 구조를 **건물에 비유**하면:
- **Layer**: 건물의 층수 (1층, 2층, 3층...)
- **Slice**: 각 층의 방 (101호, 102호, 103호...)
- **Segment**: 각 방의 구역 (침실, 거실, 주방...)

![piilot_img_1](/assets/img/piilot/piilot_img_1.jpg)

## 1단계: Layer(레이어)

Layer는 FSD 프로젝트의 **최상위 폴더**다. 총 6개의 Layer가 있으며, 각각의 역할이 명확하게 구분되어 있다.

### Layer의 핵심 규칙

> **중요한 규칙**: 상위 Layer는 하위 Layer를 참조할 수 있지만, **하위 Layer는 상위 Layer를 참조할 수 없다.**
{: .prompt-warning }

예를 들어:
- ✅ `pages`는 `features`나 `entities`의 모듈을 참조할 수 있다.
- ❌ `features`가 `pages`를 참조하는 것은 금지된다.

이 규칙을 통해 **의존성 방향**이 명확해지고, 코드의 결합도가 낮아진다.

### 6가지 Layer 설명

#### 1. App (앱 레이어)
- **역할**: 앱의 진입점 및 전역 설정
- **포함 내용**: Routing, Provider, Global Styles 등
- **예시**: `providers`, `layout`, `globals.css`
- **특징**: Slice로는 Provider, Router, Client 같은 HOC가 된다.

#### 2. Pages (페이지 레이어)
- **역할**: Route 기준으로 구성된 주요 화면 단위
- **포함 내용**: 각 주소별 페이지 컴포넌트
- **예시**: `/home`, `/about`, `/products` 등
- **특징**: 각각의 주소별 페이지가 Slice가 된다.

#### 3. Widgets (위젯 레이어)
- **역할**: 크고 독립적으로 동작하는 UI 구성 단위
- **포함 내용**: 하나의 완결된 화면 기능을 제공하는 컴포넌트
- **예시**: 헤더, 사이드바, 대시보드 카드 등
- **특징**: 여러 Feature를 묶어서 만든다. 재사용 여부에 따라 구성한다.

#### 4. Features (기능 레이어)
- **역할**: 사용자에게 비즈니스 가치를 제공하는 액션
- **포함 내용**: 재사용 가능한 제품 기능 단위
- **예시**: "좋아요", "댓글 작성", "장바구니 추가" 등
- **특징**: **동사**가 Slice가 된다. (예: `toggle-theme`, `add-to-cart`)
- **API Segment**: 해당 행동을 요청합니다.

#### 5. Entities (엔티티 레이어)
- **역할**: 프로젝트가 다루는 비즈니스 데이터
- **포함 내용**: 데이터 모델 및 관련 로직
- **예시**: `user`, `product`, `post`, `comment` 등
- **특징**: **명사**가 Slice가 된다. (예: `user`, `product`)
- **API Segment**: 데이터를 조회합니다.

#### 6. Shared (공유 레이어)
- **역할**: 모든 Layer에서 재사용되는 코드
- **포함 내용**: 라이브러리, 유틸리티, 공유 컴포넌트 등
- **예시**: `ui` (shadcn/ui 컴포넌트), `api` (Axios 인스턴스), `lib` (유틸리티)
- **특징**: Slice가 없다. 바로 Segment로 구성된다.

## 2단계: Slice(슬라이스)

Slice는 **Layer 내부를 비즈니스 도메인별로 나눈 것**이다.

### Slice의 특징

- **이름/개수 제한 없음**: 프로젝트의 도메인에 맞게 자유롭게 생성할 수 있다.
- **독립성**: 같은 Layer 내 다른 Slice를 참조할 수 없다.
- **높은 응집도, 낮은 결합도**: 관련된 코드끼리 모이고, 다른 Slice와의 의존성을 최소화한다.

### Slice 예시

- **Entities Layer**: `user`, `post`, `comment`, `product` 등
- **Features Layer**: `toggle-theme`, `add-to-cart`, `create-comment` 등
- **Pages Layer**: `home`, `about`, `products` 등

## 3단계: Segment(세그먼트)

Segment는 **Slice 내부를 코드의 역할별로 나눈 것**이다. Slice와 App/Shared Layer는 Segment로 세분화된다.

### 5가지 기본 Segment

#### 1. `ui` (UI 세그먼트)
- **역할**: UI 표현과 직접 관련된 코드
- **포함 내용**: UI 컴포넌트, 날짜 포맷터, 스타일 등
- **예시**: `Button.tsx`, `Card.tsx`, `dateFormatter.ts` 등

#### 2. `api` (API 세그먼트)
- **역할**: 백엔드 통신 및 데이터 로직
- **포함 내용**: request 함수, 데이터 타입, mappers 등
- **예시**: React Query 훅 (`useUserQuery`), API 함수 등

#### 3. `model` (모델 세그먼트)
- **역할**: 애플리케이션 도메인 모델
- **포함 내용**: schema, interfaces, store, business logic 등
- **예시**: Zustand 스토어 (`useUserStore`), 타입 정의 등

#### 4. `lib` (라이브러리 세그먼트)
- **역할**: 해당 Slice에서 여러 모듈이 함께 사용하는 공통 라이브러리 코드
- **포함 내용**: 유틸리티 함수, 헬퍼 함수 등
- **예시**: `utils.ts`, `helpers.ts` 등

#### 5. `config` (설정 세그먼트)
- **역할**: 환경/기능 설정
- **포함 내용**: configuration 파일, feature flags 등
- **예시**: `constants.ts`, `config.ts` 등

> **참고**: 대부분의 Layer에서는 위 5개 Segment로 충분하지만, 필요하다면 App 또는 Shared Layer에서만 추가 Segment를 정의할 수 있다.
{: .prompt-info }


## 실제 프로젝트 폴더구조 예시

다음은 프로젝트에서 사용하는 FSD 폴더 구조의 예시다:
``` markdown
frontend/
├── 📂 src/
│   ├── 📂 app/                 # [App Layer] 앱의 진입점 및 전역 설정
│   │   ├── 📂 providers/       # Provider 모음 (React Query, Theme 등)
│   │   │   ├── 📄 index.tsx    # 모든 Provider를 합치는 곳
│   │   │   └── 📄 theme-provider.tsx
│   │   ├── 📄 layout.tsx       # RootLayout에서 <Providers> 감싸기
│   │   ├── 📄 page.tsx         
│   │   └── 📄 globals.css      # Tailwind 기본 스타일
│   │
│   ├── 📂 pages/               # [Pages Layer] 페이지 단위 조립
│   │   └── 📂 home/            # 메인 페이지
│   │       └── 📄 ui/Page.tsx
│   │
│   ├── 📂 widgets/             # [Widgets Layer] 독립적인 UI 블록
│   │   └── 📂 header/          # 헤더 (여기에 다크모드 토글 버튼 위치)
│   │
│   ├── 📂 features/            # [Features Layer] 사용자 상호작용
│   │   ├── 📂 theme/           # 다크모드 관련 기능
│   │   │   └── 📂 ui/ThemeToggle.tsx
│   │   └── 📂 auth/            # 인증 관련
│   │
│   ├── 📂 entities/            # [Entities Layer] 비즈니스 로직 & 데이터
│   │   └── 📂 user/            
│   │       ├── 📂 api/         # React Query 훅 위치 (useUserQuery 등)
│   │       ├── 📂 model/       # Zustand 스토어 위치 (useUserStore 등)
│   │       └── 📂 lib/         # 타입 정의 (UserType 등)
│   │
│   └── 📂 shared/              # [Shared Layer] 공용 코드
│       ├── 📂 ui/              # shadcn/ui 컴포넌트 위치
│       │   ├── 📄 button.tsx
│       │   ├── 📄 card.tsx
│       │   └── 📄 ...
│       ├── 📂 api/             # Axios 인스턴스, QueryClient 설정
│       │   └── 📄 query-client.ts
│       └── 📂 lib/             # 유틸리티 (utils.ts)
│
├── 📄 components.json          # shadcn 설정 파일
├── 📄 tailwind.config.ts       # Tailwind 설정
└── 📄 package.json
```